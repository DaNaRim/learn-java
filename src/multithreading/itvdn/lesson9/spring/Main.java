package multithreading.itvdn.lesson9.spring;

public class Main {

    /*
    @Scheduled(initialDelay = 3000, fixedRate = 1000)
    @Override
    public List<Address> getAllAddresses() {
        logger.info("getAllAddresses started");

        List<Address> addresses = restTemplate.getForObject("http://localhost:8081/addresses", List.class);

        logger.info("Addresses: {}", addresses);
        logger.info("getAllAddresses finished");
        return addresses;
    }

    @Scheduled(initialDelay = 3000, fixedRate = 1000)
    @Override
    public List<ContactInfo> getAllContactInfos() {
        logger.info("getAllContactInfos started");

        List<ContactInfo> contactInfos = restTemplate.getForObject("http://localhost:8081/contactinfos", List.class);

        logger.info("ContactInfo: {}", contactInfos);
        logger.info("getAllContactInfos finished");
        return contactInfos;
    }

    @Scheduled(initialDelay = 3000, fixedRate = 1000)
    public List<Person> getAllPersons() {
        logger.info("getAllPersons started");

        List<Person> persons = restTemplate.getForObject("http://localhost:8081/persons", List.class);

        logger.info("Persons: {}", persons);
        logger.info("getAllPersons finished");
        return persons;
    }
     */
}
